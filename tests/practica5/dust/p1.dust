#include <stdio.h>
#include <stdlib.h>


{?nota_parcial }
	{#load blockid=id.nota_parcial /}
{:else}
		int main()
		{
			int a,b;
			a=b=0;
			printf("Dame nota:\n");
			scanf("%d", &a);

			printf("Dame segunda nota:\n");
			scanf("%d", &b);

			printf("Nota parcial: %f", (float)(a+b)/2);
			return 0;
		}
{/nota_parcial}
